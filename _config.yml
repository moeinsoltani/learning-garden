title: Linux Systems Learning Journal
description: Practical Linux systems — networking and virtualization, hands-on
remote_theme: just-the-docs/just-the-docs@v0.10.0

baseurl: "/learn-networking"
url: "https://moeinsoltani.github.io"

color_scheme: dark
search_enabled: true
heading_anchors: true
back_to_top: true
back_to_top_text: "Back to top"

footer_content: "Linux Systems Learning Journal"

# Apply the Just the Docs theme layout to every file (regular pages AND the
# documents in the networking/virtualization collections). Collection documents
# do NOT inherit a layout automatically, so without this they render as bare,
# unthemed HTML. An empty `path` scope matches all pages and collection docs.
defaults:
  - scope:
      path: ""
    values:
      layout: default

# Two learning tracks, each its own Just the Docs navigation section.
# Permalink keeps the in-collection path (e.g. lessons/lesson-NN) so the
# existing relative phase→lesson links keep resolving.
collections:
  networking:
    permalink: "/:collection/:path:output_ext"
    output: true
  virtualization:
    permalink: "/:collection/:path:output_ext"
    output: true

just_the_docs:
  collections:
    networking:
      name: Linux Networking
      nav_fold: true
    virtualization:
      name: Linux Virtualization
      nav_fold: true

exclude:
  - CLAUDE.md
  - "*.sh"

plugins:
  - jekyll-remote-theme
  - jekyll-seo-tag
